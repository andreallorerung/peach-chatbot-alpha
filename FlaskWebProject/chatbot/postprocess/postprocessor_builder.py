import keyword_extractor_single
import message_decorator_single
import postprocessor

def build():
    return None
    # preprocessor.MessagePostprocessor(keywordExtractor=postprocess.keyword_extractor_single.SingleKeywordExtractor(),
    #                                                         searchAdapter= the real search adapter,
    #                                                         messageDecorator=postprocess.message_decorator_single.MessageDecoratorSingle())
